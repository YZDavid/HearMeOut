import React from 'react';
import Axios from 'axios';

function PostForm() {
    return(
        <div>
            <form>
            <input placeholder = "Convert" type="text"/> 
            </form>
        </div>
    );
}

export default PostForm;